// import rewire from 'rewire'
// import sinon from 'sinon'

describe('Some test category', () => {
  beforeEach(() => {
    // do something before each test
  })

  afterEach(() => {
    // do something after each test
  })

  it('should do something', () => {
    'foo'.should.eql('foo')
  })
})
